# Build output. `python3 -m build` leaves these behind and they are not the game.
build/
dist/
*.egg-info/

# Saves and campaigns a player makes while playing out of the source tree.
*.save
*.campaign
marchlands.save
