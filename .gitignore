__pycache__/
*.pyc

# Synoptic API token (trial; never commit)
.synoptic_token

# IEM download cache written by city_regime.py -- multi-MB CSVs, regenerable
cache/

# transient ".new" staging files from the tee-safe workflow pattern
*.txt.new
